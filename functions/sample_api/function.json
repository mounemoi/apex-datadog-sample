
{
    "description": "sample code: datadog api",
    "hooks":{
        "build": "pip install -r requirements.txt -t ./site-packages"
    },
    "environment": {
        "PYTHONPATH": "/var/runtime:/var/task/site-packages"
    }
}
